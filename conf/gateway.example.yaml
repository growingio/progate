server:
  host: 0.0.0.0
  port: 8080

hashids:
  length: 8
  salt: SALT

upstreams:
  - name: metadata
    protocol: grpc
    nodes:
      - host: localhost
        port: 18080
        weight: 1


plugins:
  - hash-id

extra-plugins:
  - hash-id

graphql:
  path: "/graphql"
