openapi: 3.0.3
info:
  title: OpenAPI demo
  description: OpenAPI demo
  version: v1
paths:
  /jobs:
    summary: JobService
    description: JobService
    get:
      summary: List all jobs.
      description: List all available jobs.
      x-grpc-endpoint: growing.graphql.example.JobService/GetJob
      responses:
        default:
          description: "success"
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    id:
                      type: string
                      description: job id
                    name:
                      type: string
                      description: job name
                    args:
                      type: object
                      properties:
                        file:
                          type: string
                          description: job file path
  /jobs/{jobId}:
    summary: JobService
    description: JobService
    parameters:
      - name: jobId
        in: path
        required: true
        style: simple
    get:
      summary: List all jobs.
      description: List all available jobs.
      x-grpc-endpoint: growing.graphql.example.JobService/GetJob
      responses:
        default:
          description: "success"
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: job id
                  name:
                    type: string
                    description: job name
